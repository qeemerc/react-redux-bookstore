import * as actionTypes from '../actionTypes'

const initialState = {
  isMobile: false
}

export default (state = initialState, {type, payload}) => {
  switch(type){
    default:
      return state
  }
}